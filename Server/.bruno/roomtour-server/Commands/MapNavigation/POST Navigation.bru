meta {
  name: POST Navigation
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/chat
  body: json
  auth: none
}

body:json {
  {
    "message": "/navigate bathroom",
    "sessionId": "{{sessionId}}"
  }
}

script:post-response {
  if (res.status === 200 && res.body.sessionId) {
    bru.setVar("sessionId", res.body.sessionId);
  }
}
