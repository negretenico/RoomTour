meta {
  name: POST chat
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/chat
  body: json
  auth: none
}

body:json {
  {
    "message": "Good morning, Jeeves. What's on my schedule today?",
    "sessionId": "{{sessionId}}"
  }
}

script:post-response {
  if (res.status === 200 && res.body.sessionId) {
    bru.setVar("sessionId", res.body.sessionId);
  }
}
